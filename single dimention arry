#include<stdio.h>
int main ()
{
    int i,j,a[3];
    
    printf("enter the no of elements:");
    scanf("%d",&j);
    for(i=0;i<j;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<j;i++)
    {
        printf("%d \n",a[i]);
    }
}
